# ImageReader

## Usage

Use command below to execute this program:

```bash
cd src
chmod +x run.sh
./run.sh
```

## Test

Use command below to execute test program:

```bash
cd src
chmod +x test.sh
./test.sh
```
